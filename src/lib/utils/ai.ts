import { TemporaryChatMessage } from "../types";

export async function getAIResponse(prompt: string, imageURI?: string): Promise<string> {
  // call an self hosted edge function API or something

  return '';
}

export async function getChatTitle(messages: TemporaryChatMessage[]): Promise<string> {
  // call an self hosted edge function API or something

  return '';
}