// TODO: MOVE TO EDGE FUNCTION

export function getOregonRecyclingCode(): string {
  // if multiple regions within oregon have different recycling codes, write the dynamic search code. use web search APIs & AI APIs to do this

  return '';
}

export function getRecyclingCode(location: string): string {
  // use web search APIs + AI APIs to find user's recycling code
  // return the recycling code

  return '';
}